<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<settings version="1">
  <section id="script.eon.defaults">
    <category id="general" label="30000">
      <group id="1">
        <setting id="enabled" type="boolean" label="30001" help="30002">
          <level>0</level>
          <default>true</default>
          <control type="toggle"/>
        </setting>
        <!-- Written by the service, not by the user: it is what makes the
             values a starting point rather than something re-imposed on every
             start. Hidden because there is nothing useful to do with it by
             hand; switching "enabled" off and on clears it. -->
        <setting id="applied" type="boolean" label="30003">
          <level>4</level>
          <default>false</default>
          <control type="toggle"/>
          <visible>false</visible>
        </setting>
      </group>
    </category>
  </section>
</settings>
